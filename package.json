{
  "name": "hats-frontend",
  "private": true,
  "scripts": {
    "dev": "cd apps/shared && yarn build && cd ../.. && cd apps/web && yarn start",
    "publish:shared": "cd apps/shared && yarn run publish"
  },
  "workspaces": [
    "apps/*"
  ]
}