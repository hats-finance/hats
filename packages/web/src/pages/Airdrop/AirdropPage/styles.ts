import styled from "styled-components";
import { getSpacing } from "styles";

export const StyledAidropPage = styled.div`
  h2.subtitle {
    display: flex;
    align-items: center;
    gap: ${getSpacing(2)};
  }
`;
